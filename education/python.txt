https://pythononline.ru/osnovy/parsing-xml-python Парсинг XML
beautifulsoup4 4.10.0 - PyPI парсинг HTML

from time import perf_counter //замер времени выполения
with opent('some file.txt') as f:
f.tell() - возвращает текующи позицию при чтении
f.seek(offset, whence) - поменять текущую позицию whence 0- начало файла, 1-текущая позиция, 2-конец

json.dumps(nums) - сериализация в json //json.dump(nums, f) позволяет сразу записать в файл
json.dumps({'4': 5, '6': 7}, sort_keys=True, indent=4)

json.loads(nums_as_str) - десериализация //nums = json.load(f) сразу считать и дерериализовать

from time import perf_counter // Профилирование времени
start = perf_counter()
print(f'json saved: {perf_counter() - start}')

ord('d') <-> chr(100)

b'bytes' = 'Байты'.encode('utf-8') = bytes('bytes', encoding = 'utf-8')
bytearray(b'hello world!') в отличии от bytes изменяемый

import requests
with requests.get(url, stream=True) as r:
response = requests.request('GET', url) or response = requests.get(url)
encodings = requests.utils.get_encoding_from_headers(response.headers)
content = response.content.decode(encoding=encodings)
https://docs.python-requests.org/en/master/api/

регулярные выражения https://habr.com/ru/post/349860/